{
  "name": "happy-sheep-farm-android",
  "version": "1.0.0",
  "private": true,
  "description": "Android app wrapper for Happy Sheep Farm",
  "scripts": {
    "sync": "cap sync android",
    "build:debug": "cap sync android && cd android && gradlew.bat assembleDebug",
    "build:release": "cap sync android && cd android && gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "6.2.1",
    "@capacitor/core": "6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "6.2.1"
  }
}
